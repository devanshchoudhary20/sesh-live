# Relay origin (shared name with apps/landing's .env.example); ws(s):// is derived from this at runtime
VITE_RELAY_ORIGIN=http://localhost:8787

# Optional: where the "Get the link for your own session" CTA sends visitors
VITE_LANDING_URL=/
